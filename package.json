{
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "rm -rf docs/.vitepress/dist && vitepress build docs && node scripts/generate-legacy-redirects.mjs && node scripts/check-seo.mjs",
    "docs:seo-check": "node scripts/check-seo.mjs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "markdown-it-emoji": "^3.0.0",
    "mermaid": "^11.12.3",
    "vitepress": "^1.6.4",
    "vitepress-openapi": "^0.1.14",
    "vitepress-plugin-mermaid": "^2.0.17"
  },
  "dependencies": {
    "medium-zoom": "^1.1.0"
  },
  "overrides": {
    "esbuild": "0.25.12"
  }
}
